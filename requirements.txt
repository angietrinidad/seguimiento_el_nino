# Dependencias para renderizar el sitio con Quarto.
# Las tablas usan la librería estándar; el mapa de riesgo usa folium.
jupyter>=1.0
pyyaml>=6.0
nbformat>=5.0
nbclient>=0.7
folium>=0.20

# Solo para REGENERAR la capa de agua satelital (scripts/derivar_huella_agua.py);
# no hacen falta para renderizar el sitio:
# pystac-client>=0.7
# planetary-computer>=1.0
# rioxarray>=0.15
# rasterio>=1.3
# shapely>=2.0
# pyproj>=3.5

# Descomentar si se agregan gráficos:
# pandas>=2.0
# matplotlib>=3.7
